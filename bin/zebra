#!/usr/bin/env perl
# pretty print spaced lines while removing duplicate blank lines

while (<>) {
	if ($_ !~ /^$/) {
		print $n ? "\n" : '' , $_;
		$n=1; # very first line will have no preceeding \n
	}
}
